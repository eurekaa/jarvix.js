(function() {
  var jarvix_memory;

  jarvix_memory = typeof window !== 'undefined' ? window['jarvix_memory'] : global['jarvix_memory'];

  jarvix_memory.module.define(['underscore'], function(_) {});

}).call(this);
